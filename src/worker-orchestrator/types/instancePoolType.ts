export type VM_INSTANCE = {
    instanceIP: string,
    projectID: string,
    isUsed: boolean,
}

export type ASG_DETAILS = {
    MinSize: number,
    MaxSize: number,
    DesiredCapacity: number,

}

